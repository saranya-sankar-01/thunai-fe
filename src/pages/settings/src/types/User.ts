export interface User {
  user_id: string;
  username: string;
  emailid: string;
  default_tenant_id: string;
  allowed_tenants: string;
  role: string;
  status: string;
  created: string;
  updated: string;
}
